<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iOS Screen Capture Tool"
        Width="674"
        Height="630"
        MinWidth="700"
        MinHeight="300"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3">
    <Window.Resources>
        <SolidColorBrush x:Key="ThemeCardBrush" Color="#FCFCFC" />
        <SolidColorBrush x:Key="ThemeTextPrimaryBrush" Color="#1C1C1C" />
        <SolidColorBrush x:Key="ThemeBorderBrush" Color="#26000000" />
        <SolidColorBrush x:Key="ThemeHoverBrush" Color="#F0F0F0" />
        <SolidColorBrush x:Key="ThemeSelectedBrush" Color="#E6E6E6" />
        <SolidColorBrush x:Key="ThemeScrollBarTrackBrush" Color="#F5F5F5" />
        <SolidColorBrush x:Key="ThemeScrollBarThumbBrush" Color="#C8C8C8" />
        <SolidColorBrush x:Key="ThemeScrollBarThumbHoverBrush" Color="#B4B4B4" />
        <SolidColorBrush x:Key="ThemeScrollBarThumbPressedBrush" Color="#A0A0A0" />
        <ControlTemplate x:Key="Windows11ButtonTemplate" TargetType="Button">
            <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Opacity" Value="0.9" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Opacity" Value="0.8" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="border" Property="Opacity" Value="0.5" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource ThemeCardBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.25" />
                            </Border.Effect>
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4" />
                    <Setter Property="IsHitTestVisible" Value="False" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ThemeHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ThemeHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ThemeSelectedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{DynamicResource ThemeScrollBarTrackBrush}" />
            <Setter Property="Width" Value="2" />
            <Setter Property="Opacity" Value="0" />
            <Setter Property="IsHitTestVisible" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0" IsHitTestVisible="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="{DynamicResource ThemeScrollBarThumbBrush}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0" IsHitTestVisible="False" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ScrollViewer}}" Value="True">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="IsHitTestVisible" Value="True" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder" Background="{TemplateBinding Background}" CornerRadius="2" Margin="1" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ThemeScrollBarThumbHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ThemeScrollBarThumbPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="rootGrid" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="streamHostGrid" Grid.Column="0" Margin="0,0,18,0">
            <Border x:Name="streamCardBorder"
                    Background="Black"
                    BorderBrush="#26000000"
                    BorderThickness="1"
                    CornerRadius="8">
                <Image x:Name="previewImage" Stretch="Uniform" />
            </Border>
        </Grid>

        <ScrollViewer x:Name="rightHostGrid" Grid.Column="1" Margin="6,0,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Expander IsExpanded="True" Margin="0,0,0,16">
                <Expander.Header>
                    <TextBlock x:Name="streamTitleText"
                               Text="Stream"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="#1C1C1C" />
                </Expander.Header>
                <Border x:Name="streamInfoCardBorder"
                        Margin="0,10,0,0"
                        Padding="18,16,18,14"
                        CornerRadius="8"
                        BorderBrush="#1F000000"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="statusText"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Text="Idle"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="#1C1C1C" />
                        <ComboBox x:Name="fpsComboBox"
                                  Grid.Column="1"
                                  Margin="10,0,10,0"
                                  VerticalAlignment="Center"
                                  Width="90"
                                  SelectedValuePath="Tag"
                                  SelectedValue="30">
                            <ComboBoxItem Content="1 fps" Tag="1" />
                            <ComboBoxItem Content="5 fps" Tag="5" />
                            <ComboBoxItem Content="25 fps" Tag="25" />
                            <ComboBoxItem Content="30 fps" Tag="30" />
                            <ComboBoxItem Content="60 fps" Tag="60" />
                        </ComboBox>
                        <Button x:Name="captureNowButton"
                                Grid.Column="2"
                                Width="42"
                                Height="42"
                                ToolTip="Grab Screenshot"
                                Template="{StaticResource Windows11ButtonTemplate}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE114;" FontSize="20" />
                        </Button>
                    </Grid>
                </Border>
            </Expander>

            <Expander IsExpanded="True" Margin="0,0,0,16">
                <Expander.Header>
                    <TextBlock x:Name="capturedFramesTitleText"
                               Text="Captured Frames"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="#1C1C1C" />
                </Expander.Header>
                <Border x:Name="capturedFramesCardBorder"
                        Margin="0,10,0,0"
                        Padding="18,16,18,16"
                        CornerRadius="8"
                        BorderBrush="#1F000000"
                        BorderThickness="1">
                    <ListBox x:Name="capturedFramesListBox"
                             Height="150"
                             Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,8,8"
                                        Padding="2"
                                        ToolTip="{Binding FileName}">
                                    <Border.ContextMenu>
                                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Header="Copy image location" Click="CopyImageLocation_Click" />
                                            <MenuItem Header="Open file location" Click="OpenFileLocation_Click" />
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <Image Source="{Binding Thumbnail}"
                                           MaxWidth="72"
                                           MaxHeight="72"
                                           Stretch="Uniform" />
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <EventSetter Event="MouseDoubleClick" Handler="CapturedFrame_MouseDoubleClick" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Border>
            </Expander>

            <Expander Margin="0,0,0,16">
                <Expander.Header>
                    <TextBlock x:Name="settingsTitleText"
                               Text="Settings"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="#1C1C1C" />
                </Expander.Header>
                <Border x:Name="settingsCardBorder"
                        Margin="0,10,0,0"
                        Padding="18,16,18,16"
                        CornerRadius="8"
                        BorderBrush="#1F000000"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock x:Name="captureFolderHeaderText"
                                   Text="Capture folder"
                                   FontWeight="SemiBold"
                                   Foreground="#565656" />
                        <Grid Margin="0,8,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="captureFolderTextBox"
                                     Grid.Column="0"
                                     Height="32"
                                     Margin="0,0,10,0"
                                     VerticalContentAlignment="Center"
                                     IsReadOnly="True" />
                            <Button x:Name="browseCaptureFolderButton"
                                    Grid.Column="1"
                                    Width="130"
                                    Height="32"
                                    Content="Browse..."
                                    Template="{StaticResource Windows11ButtonTemplate}" />
                        </Grid>
                        <CheckBox x:Name="startWithWindowsCheckBox"
                                  Margin="0,6,0,0"
                                  Content="Start on startup" />
                        <CheckBox x:Name="keepMinimizedCheckBox"
                                  Margin="0,6,0,0"
                                  Content="Keep minimized after closing" />
                    </StackPanel>
                </Border>
            </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
